/**
 * Состояние загрузки блока экрана.
 *
 * Три обязательных состояния — загрузка, пустота и ошибка — различаются именно здесь:
 * пустота выводится из готовых данных, а вот «идёт» и «не прочиталось» вывести
 * не из чего, и они приходят свойством.
 */
export type LoadState = 'loading' | 'error' | 'ready';
