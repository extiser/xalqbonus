/**
 * Вариант выбора: что отправится и что прочитает человек.
 *
 * Лежит отдельно от компонента, потому что читают его двое — само поле и тот, кто собирает
 * ему список, — а `<script setup>` типов наружу не отдаёт.
 */
export type SelectOption = {
  value: string;
  label: string;
};
