node_modules/
.nuxt/
.output/
# Клиент Prisma генерируется в дерево исходников (prisma/schema.prisma → generator.output).
server/generated/
dist/
.env
.env.*
!.env.example
*.log
logs/
.DS_Store
/data/
# Правила `*.sql` здесь нет намеренно. Оно закрывало наши же миграции и запросы,
# требуя цепочки исключений на каждый новый файл, и однажды чуть не съело первую
# миграцию проекта. Дампы и выгрузки закрыты своими строками ниже — этого хватает.

# дампы продовой базы
_backup/
*.dump

# выгрузки Fleet API: персональные данные парка, в репозиторий не попадают
_reference/fleet-api/dumps/

# байт-код питоновских скриптов разведки
__pycache__/
