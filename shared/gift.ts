/**
 * Правила раздачи подарка, общие для сервера и формы (issue #219).
 *
 * Лежат в `shared/` по той же причине, что ограничения фото: форма собирает запрос по этим
 * именам и называет размер обложки, а ручка разбирает запрос по ним же.
 */

/**
 * Поле с обложкой в теле `multipart/form-data`. Остальные поля идут строками рядом, по именам
 * `GiftGrantRequestBody`. Файла нет — раздача без обложки.
 */
export const GIFT_COVER_FIELD = 'cover';

/**
 * Размер обложки — подсказкой под полем. Пропорции не проверяются: рамка 16:9 с обрезкой
 * по краям в приложении сама не даёт вертикальной картинке вытянуть экран
 * (`_reference/design/gifts/gifts-cover.md`).
 */
export const GIFT_COVER_SIZE_HINT = 'Горизонтальная, 1280 × 720 (16:9)';
