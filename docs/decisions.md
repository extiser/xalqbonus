# Принятые технические решения

Источник истины по стеку и версиям. При расхождении с CLAUDE.md верить этому файлу.

## Стек

| Слой | Выбор | Версия | Почему |
|---|---|---|---|
| Фреймворк | Nuxt + Nitro | 4.x / 2.x | админка, Mini App и API в одном проекте, один деплой |
| Язык | TypeScript | 5.x | закрывает класс багов «поле есть в БД, нет в модели» |
| ORM | Prisma | 6.x | поднимается на существующей схеме через introspection |
| БД | PostgreSQL | 16 | та же, что на проде |
| Очереди | BullMQ | 5.x | ретраи, наблюдаемая очередь, расписания |
| Кэш/очередь | Redis | 7 | под BullMQ и сессии |
| Бот | Telegraf | 4.x | остаётся из старого проекта, знакомый API |
| Стили | TailwindCSS | 4.x | как в остальных проектах |
| Тесты | Vitest | 3.x | как в остальных проектах |

Версии фиксируются точными значениями при `npm install` на этапе 0
и дописываются сюда.

## Отвергнутые альтернативы

**Оставить Express + Sequelize, причесать.** Не даёт админку и Mini App
без второго проекта, оставляет расхождения модель↔БД, не решает очереди.

**Полностью новая БД с переносом данных.** Лишний риск на старте.
Работаем с той же схемой, дополняя её; данные не переезжают.

**node-cron вместо BullMQ.** Не даёт ретраев, видимости очереди и параллельных
воркеров. Старый проект на этом и споткнулся: одна упавшая задача теряется молча.

## Решения

### Баланс — журнал, а не число
Единственный способ изменить баланс — запись в `point_entries` с уникальным
`idempotency_key`. Подробности и схема ключей — `domain/points.md`.

### Mini App как основной интерфейс
Водительский и админский UI уезжают в Telegram Mini App и веб. В боте остаётся
первый контакт, привязка `chat_id`, кнопка запуска и уведомления. Следствие:
~60 callback-обработчиков старого проекта не переписываются, они исчезают.

Побочный эффект — авторизация чинится архитектурно: `initData` подписан токеном
бота и проверяется по HMAC на сервере, личность приходит от Telegram,
а не из клиентского `callback_data`.

### Порты смещены
PostgreSQL 5433, Redis 6380 — чтобы не конфликтовать с локальными сервисами
других проектов.

## Открытые вопросы

- Точные версии пакетов — после этапа 0
- Нужен ли отдельный процесс для воркеров или хватит Nitro-плагина — после этапа 3
- Хостинг Mini App: тот же домен, что API, или отдельный — после этапа 5
