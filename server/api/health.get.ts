// Здоровье процесса: отвечает любой запустившийся экземпляр. В зависимости не ходит намеренно —
// в эту ручку бьёт healthcheck образа, и падение базы не должно перезапускать приложение.
export default defineEventHandler(() => ({ status: 'ok' }));
