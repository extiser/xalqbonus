/**
 * Нормализация номера телефона на сервере.
 *
 * Правило одно на весь проект и живёт в `shared/phone.ts`: его же читает экран, показывая
 * телефон водителю. Здесь — точка, через которую его берут регистрация, синхронизация
 * и вход сотрудника: реестр наполняет прогон профилей, а ищет в нём регистрация, и разойдись
 * эти две нормализации — привязка перестанет находить того, кого прогон только что записал.
 */

// Относительным путём, а не через `#shared`: модуль собирается ещё и в воркер (прогон реестра),
// а там из псевдонимов настроен один `#server` (package.json → `build:worker`).
export { normalizePhoneE164 } from '../../shared/phone';
