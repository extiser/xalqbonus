/**
 * Статус завершённой поездки — единственный, за который начисляется балл и от которого
 * считается давность поездки у сегментов. Значение из словаря Fleet API.
 *
 * Одной константой на весь сервер: перечисления в базу на чужой словарь не заводятся
 * (docs/decisions.md → «Перечисления заводим только на свои словари»), и держит значение
 * только код. Пять копий строки — начисление, синхронизация, сводка, чтение поездок и отбор
 * сегмента — разошлись бы на первой правке, и разошлись бы тихо: сегмент перестал бы видеть
 * поездки, за которые баллы начисляются.
 */
export const COMPLETED_TRIP_STATUS = 'complete';
